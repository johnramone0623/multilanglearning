# 部署决策树 & 故障排查 🌳

## 🎯 选择你的部署方案

```
开始部署
    |
    ├─ 你有GitHub账号吗？
    │   │
    │   ├─ 有 ─→ 你会用Git吗？
    │   │       │
    │   │       ├─ 会 ─→ 【推荐】Vercel自动部署
    │   │       │         └─ 见方案A
    │   │       │
    │   │       └─ 不会 ─→ GitHub网页上传 + Vercel
    │   │                 └─ 见方案B
    │   │
    │   └─ 没有 ─→ 注册GitHub（2分钟）
    │             └─ 然后回到上面
    │
    └─ 不想用GitHub？
        │
        ├─ Netlify拖放部署
        │   └─ 见方案C
        │
        └─ 有自己的服务器？
            └─ 服务器部署
                └─ 见方案D
```

---

## 📋 方案A：Vercel自动部署（推荐）

### 适合人群
✅ 会用Git命令
✅ 想要自动部署
✅ 追求最佳实践

### 完整流程

```
本地电脑
    ↓
[1] 安装Git
    ↓
[2] 配置Git用户信息
    git config --global user.name "你的名字"
    git config --global user.email "your@email.com"
    ↓
[3] 初始化仓库
    cd multilang-learning-app
    git init
    git add .
    git commit -m "Initial commit"
    ↓
[4] 创建GitHub仓库
    访问 github.com/new
    创建仓库: learning-app
    复制仓库URL
    ↓
[5] 连接远程仓库
    git remote add origin https://github.com/你的用户名/learning-app.git
    git branch -M main
    git push -u origin main
    ↓
    【如果提示输入密码，使用Personal Access Token】
    ↓
GitHub
    ↓
[6] 注册Vercel
    访问 vercel.com
    用GitHub登录
    ↓
[7] 导入项目
    Add New → Project
    选择 learning-app
    Import
    ↓
[8] 配置环境变量 ⚠️ 关键步骤
    Name: VITE_GEMINI_API_KEY
    Value: AIzaSyXXXXXX
    勾选所有环境
    ↓
[9] 部署
    点击 Deploy
    等待 2-3分钟
    ↓
    ✅ 完成！
    获得URL: https://learning-app.vercel.app
```

### 时间线
- 步骤1-5: 5分钟
- 步骤6-9: 5分钟
- **总计: 10分钟**

---

## 📋 方案B：GitHub网页上传 + Vercel

### 适合人群
✅ 不会用Git命令
✅ 只需要部署一次
✅ 追求简单

### 完整流程

```
本地准备
    ↓
[1] 确保所有文件都在 multilang-learning-app 文件夹
    检查清单：
    □ package.json
    □ index.html
    □ src/ 文件夹
    □ public/ 文件夹
    □ README.md
    ↓
GitHub网页
    ↓
[2] 登录 github.com
    没有账号？点击 Sign up 注册
    ↓
[3] 创建新仓库
    点击 + → New repository
    Repository name: learning-app
    选择 Public
    ❌ 不要勾选任何选项
    Create repository
    ↓
[4] 上传文件
    点击 "uploading an existing file"
    打开 multilang-learning-app 文件夹
    选择所有文件（Ctrl+A 或 Cmd+A）
    拖拽到网页
    等待上传（可能需要1-2分钟）
    ↓
[5] 提交
    Commit message: "Initial commit"
    Commit changes
    ↓
Vercel
    ↓
[6-9] 同方案A的步骤6-9
    ↓
    ✅ 完成！
```

### 时间线
- 步骤1-5: 5分钟
- 步骤6-9: 5分钟
- **总计: 10分钟**

---

## 📋 方案C：Netlify拖放部署

### 适合人群
✅ 想最快部署
✅ 不在意自动更新
✅ 临时测试

### 完整流程

```
本地构建
    ↓
[1] 打开命令行
    cd multilang-learning-app
    ↓
[2] 创建 .env 文件
    echo VITE_GEMINI_API_KEY=AIzaSyXXXX > .env
    ↓
[3] 安装依赖
    npm install
    等待 1-2分钟
    ↓
[4] 构建
    npm run build
    等待 30秒
    生成 dist/ 文件夹
    ↓
Netlify
    ↓
[5] 注册 Netlify
    访问 netlify.com
    Sign up → 用GitHub登录
    ↓
[6] 拖放部署
    Sites → 拖拽 dist 文件夹到页面
    等待上传
    ↓
    ✅ 完成！
    获得URL: https://random-name-123.netlify.app
```

### 优点 vs 缺点

**优点：**
- 最快（5分钟）
- 最简单
- 不需要Git

**缺点：**
- 每次更新都要重新构建和拖放
- 没有版本控制
- 不够专业

### 时间线
- **总计: 5分钟**

---

## 📋 方案D：自己的服务器

### 适合人群
✅ 有VPS服务器
✅ 想完全控制
✅ 了解Linux

### 完整流程

```
准备服务器
    ↓
[1] SSH连接
    ssh root@your-server-ip
    ↓
[2] 安装依赖
    # Node.js
    curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
    sudo apt-get install -y nodejs
    
    # Nginx
    sudo apt install nginx -y
    ↓
[3] 上传代码
    方法1: git clone https://github.com/你的用户名/learning-app.git
    方法2: scp -r multilang-learning-app/ user@server:/var/www/
    ↓
[4] 配置环境
    cd /var/www/multilang-learning-app
    nano .env
    # 添加: VITE_GEMINI_API_KEY=XXX
    ↓
[5] 构建
    npm install
    npm run build
    ↓
[6] 配置Nginx
    sudo nano /etc/nginx/sites-available/learning-app
    # 粘贴配置（见DEPLOYMENT_DETAILED.md）
    sudo ln -s /etc/nginx/sites-available/learning-app /etc/nginx/sites-enabled/
    sudo nginx -t
    sudo systemctl restart nginx
    ↓
[7] 配置SSL（可选但推荐）
    sudo apt install certbot python3-certbot-nginx
    sudo certbot --nginx -d your-domain.com
    ↓
    ✅ 完成！
    访问: https://your-domain.com
```

### 时间线
- **总计: 30-60分钟**

---

## 🔍 故障排查流程图

### 问题：网站打不开

```
网站打不开
    |
    ├─ 显示 404 Not Found
    │   |
    │   ├─ Vercel/Netlify
    │   │   └─ 检查构建是否成功
    │   │       └─ Deployments → 查看Build Logs
    │   │
    │   └─ 自己的服务器
    │       └─ 检查Nginx配置
    │           └─ sudo nginx -t
    │
    ├─ 显示空白页
    │   |
    │   └─ 打开浏览器控制台（F12）
    │       |
    │       ├─ 看到 "API key not found"
    │       │   └─ 环境变量没配置
    │       │       └─ 添加 VITE_GEMINI_API_KEY
    │       │           └─ Redeploy
    │       │
    │       └─ 看到其他错误
    │           └─ 复制错误信息
    │               └─ Google搜索
    │
    └─ 完全无法访问
        |
        ├─ 检查域名DNS
        │   └─ nslookup your-domain.com
        │
        └─ 检查服务器防火墙
            └─ sudo ufw status
```

### 问题：推送到GitHub失败

```
git push 失败
    |
    ├─ "Authentication failed"
    │   |
    │   └─ 使用Personal Access Token
    │       |
    │       [1] GitHub → Settings → Developer settings
    │       [2] Personal access tokens → Tokens (classic)
    │       [3] Generate new token
    │       [4] 勾选 repo 权限
    │       [5] 复制token
    │       [6] 用token作为密码重试
    │
    ├─ "Permission denied"
    │   |
    │   └─ 设置正确的remote URL
    │       └─ git remote set-url origin https://token@github.com/user/repo.git
    │
    └─ "fatal: not a git repository"
        |
        └─ 初始化Git
            └─ git init
```

### 问题：构建失败

```
Vercel/Netlify构建失败
    |
    [1] 查看构建日志
        └─ 找到报错的行
    |
    ├─ "Module not found"
    │   └─ package.json缺少依赖
    │       └─ npm install 缺少的包
    │           └─ git add, commit, push
    │
    ├─ "Build failed"
    │   └─ 检查构建命令
    │       └─ 应该是: npm run build
    │
    └─ "Out of memory"
        └─ 减小依赖或升级计划
```

### 问题：功能不工作

```
某个功能不工作
    |
    ├─ 生成题目失败
    │   |
    │   ├─ API密钥错误
    │   │   └─ 检查环境变量
    │   │       └─ VITE_GEMINI_API_KEY
    │   │
    │   ├─ 超过速率限制
    │   │   └─ 等待1分钟后重试
    │   │
    │   └─ API密钥失效
    │       └─ 重新生成密钥
    │
    ├─ 拍照上传不工作
    │   |
    │   └─ 检查是否HTTPS
    │       |
    │       ├─ Vercel/Netlify自动HTTPS ✅
    │       └─ 自己服务器需要配置SSL ⚠️
    │
    └─ 词汇导入失败
        |
        └─ 检查Excel格式
            └─ 必须有"单词"列
```

---

## 📊 部署对比表

| 特性 | Vercel | Netlify | 服务器 |
|------|--------|---------|--------|
| 难度 | ⭐ 简单 | ⭐ 简单 | ⭐⭐⭐ 复杂 |
| 时间 | 10分钟 | 5分钟 | 30-60分钟 |
| 费用 | 免费 | 免费 | $5-10/月 |
| 自动部署 | ✅ 有 | ✅ 有 | ❌ 需手动 |
| HTTPS | ✅ 自动 | ✅ 自动 | ⚠️ 需配置 |
| 域名 | ✅ 免费子域名 | ✅ 免费子域名 | ⚠️ 需购买 |
| CDN | ✅ 全球 | ✅ 全球 | ❌ 无 |
| 控制权 | 🟡 部分 | 🟡 部分 | ✅ 完全 |
| 推荐度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## ✅ 部署检查清单

### 部署前

- [ ] 本地能正常运行（`npm run dev`）
- [ ] 有Gemini API密钥
- [ ] 有GitHub账号
- [ ] 代码已提交（Git用户）

### 部署中

- [ ] 环境变量已配置（VITE_GEMINI_API_KEY）
- [ ] 构建命令正确（npm run build）
- [ ] 输出目录正确（dist）
- [ ] 构建成功（没有错误）

### 部署后

- [ ] 网站能打开
- [ ] 首页正常显示
- [ ] 生成题目功能正常
- [ ] 词汇导入功能正常
- [ ] 拍照上传功能正常（HTTPS）
- [ ] 手机访问正常
- [ ] 能添加到主屏幕

---

## 🎓 部署术语解释

| 术语 | 解释 |
|------|------|
| **部署 (Deploy)** | 把代码放到服务器，让别人能访问 |
| **构建 (Build)** | 把源代码转换成可运行的网站 |
| **环境变量 (Environment Variable)** | 配置信息，如API密钥 |
| **CDN** | 内容分发网络，让网站更快 |
| **HTTPS** | 加密的HTTP，更安全 |
| **域名 (Domain)** | 网站地址，如 example.com |
| **DNS** | 把域名转换成IP地址的系统 |
| **Git** | 代码版本控制工具 |
| **GitHub** | 代码托管平台 |
| **Vercel** | 网站部署平台 |
| **SSL证书** | 实现HTTPS的证书 |

---

## 🔗 快速链接

### 必需服务

- [GitHub](https://github.com) - 代码托管
- [Vercel](https://vercel.com) - 部署平台
- [Gemini API密钥](https://makersuite.google.com/app/apikey) - AI服务

### 学习资源

- [Git教程](https://www.liaoxuefeng.com/wiki/896043488029600) - 廖雪峰
- [GitHub教程](https://docs.github.com/zh/get-started) - 官方文档
- [Vercel文档](https://vercel.com/docs) - 官方文档

### 工具

- [测试网站速度](https://pagespeed.web.dev)
- [检查DNS](https://www.whatsmydns.net)
- [生成favicon](https://favicon.io)

---

**根据你的情况选择合适的方案，开始部署吧！** 🚀
